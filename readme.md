# Littlebuild

My own C pseudo build system for personal use, created because Make's syntax is too cryptic for my taste.

- **`littlebuild.py`**: The core module; it contains compilation and linking functions.
- **`build.py`**: The script you actually run to build your project.

A minimal "Hello, World!" example is included, and all available functions are well documented.